# Concat Sequences

In this exercise, you will use `concat` to combine sequences.

## Task

Write a function called `concat-two` that takes two sequences and returns their concatenation using `concat`.

For example:
```clojure
(concat-two [1 2] [3 4])
; => (1 2 3 4)
```