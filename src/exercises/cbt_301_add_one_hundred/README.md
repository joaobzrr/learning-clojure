# Add One Hundred

In this exercise, you will write a simple function that performs arithmetic.

## Task

Write a function called `add-100` that takes a single argument (a number) and returns that number plus 100.