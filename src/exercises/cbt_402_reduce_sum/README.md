# Reduce Sum

In this exercise, you will use `reduce` to compute the sum of a sequence of numbers.

## Task

Write a function called `sum-all` that takes a sequence of numbers and returns their sum using the `reduce` function.

For example:
```clojure
(sum-all [1 2 3 4])
; => 10
```