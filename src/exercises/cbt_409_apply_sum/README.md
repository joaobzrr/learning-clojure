# Apply Sum

In this exercise, you will use `apply` to apply a function to a sequence of arguments.

## Task

Write a function called `sum-with-apply` that takes a sequence of numbers and returns their sum using `apply` with `+`.

For example:
```clojure
(sum-with-apply [1 2 3 4])
; => 10
```